# 安全评估引擎（工具风险分级 + 依赖漏洞审计）
# 二者是「工具安全控制机制」内部的评估引擎，而非被管控的业务工具。
bandit==1.9.4        # Apache-2.0，PyCQA 静态安全扫描（离线可用）
pip-audit==2.10.1    # Apache-2.0，Trail of Bits 依赖漏洞审计（需联网，离线自动降级）

# 可选扩展（接入真实 LLM 智能体入口时按需安装）：
# nanobot
# fastapi
# uvicorn
