# .env 示例 —— 复制为 .env 并填入真实值（.env 已被 .gitignore 忽略，请勿提交真实密钥）
# 说明：agent 会从项目目录向上查找 .env；未配置时自动回退到关键词路由（离线可用）。

# DeepSeek API 配置（OpenAI 兼容接口，用于 LLM 意图路由）
DEEPSEEK_API_KEY=sk-xxxxxx
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-chat
